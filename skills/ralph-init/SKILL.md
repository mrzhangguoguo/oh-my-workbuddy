---
name: ralph-init
category: shortcut
status: deprecated
description: "Deprecated. Do not invoke. Use ralph directly after PRD/test-spec planning is complete."
agent_created: true
triggers: ["ralph-init"]
---

> Ported from oh-my-codex `ralph-init`. OMX runtime conventions (`$macro` invocation, `omx` CLI, `.omx/` state directory) are replaced with WorkBuddy idioms (Skill tool, Agent tool, task list, `.workbuddy/memory`).

# Ralph Init (deprecated)

Deprecated. Do not invoke or route this skill. Use the `ralph` skill directly after PRD/test-spec planning is complete.
