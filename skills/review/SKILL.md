---
name: review
category: review
status: deprecated
description: "Deprecated. Do not invoke. Use code-review directly for implementation review, or plan --review only when reviewing an existing planning artifact."
agent_created: true
triggers: ["review"]
---

> Ported from oh-my-codex `review`. OMX runtime conventions (`$macro` invocation, `omx` CLI, `.omx/` state directory) are replaced with WorkBuddy idioms (Skill tool, Agent tool, task list, `.workbuddy/memory`).

# Review (deprecated)

Deprecated. Do not invoke or route this skill. Use the `code-review` skill directly for implementation review, or `plan --review` only when explicitly reviewing an existing planning artifact.
