---
name: visual-verdict
category: review
status: deprecated
description: 'Deprecated. Visual verdict scoring is now part of the `visual-ralph` workflow; do not invoke this skill directly. Triggers: "visual verdict", "visual-ralph verdict".'
agent_created: true
triggers: ["visual verdict", "visual-ralph verdict"]
---

> Ported from oh-my-codex `visual-verdict`. OMX runtime conventions (`$macro` invocation,
> `omx` CLI, `.omx/` state directory) are replaced with WorkBuddy idioms
> (Skill tool, Agent tool, task list, `.workbuddy/memory`).

# Visual Verdict — deprecated

Hard-deprecated. Do not invoke or route this skill. Use the `visual-ralph` skill directly; visual
verdict scoring is part of that workflow (its "Visual Ralph verdict" step).
